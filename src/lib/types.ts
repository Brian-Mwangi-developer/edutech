export type discussionProps= {
  icon: string;
  description: string;
  prompt: string;
  summaryPrompt: string;
  name: string;
}

export type coachingProps={
  name:string,
  avatar:string,
  voice:string
}